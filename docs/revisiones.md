# Registro de revisiones

Cada revisión cruzada entre Claude Code y Codex se anota aquí. Cada dos entregas, el orquestador lo resume y propone a Dani ajustes del reparto de trabajo basados en lo que cada agente ha detectado de verdad.

Niveles: **B** bloqueante · **I** importante · **S** sugerencia. «Aceptados» cuenta los hallazgos que se corrigieron.

| Fecha | Entrega · tarea | Implementa | Revisa | B | I | S | Aceptados | Notas |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-22 | 2.5 · T0 | Claude Code | — | 0 | 0 | 0 | — | Codex (MCP) sin conexión (`CONNECTION_CLOSED`). No hubo revisión cruzada; se avisa a Dani. |
| 2026-09-23 | 2.5 · T0 (prueba del script) | Claude Code | Codex (CLI, `npm run revision`, esfuerzo low) | 0 | 0 | 0 | — | Primera ejecución de `scripts/windows/revision.ps1`: funcionó sin ajustes (sin fallos de sintaxis PowerShell ni de redirección en cmd.exe), terminó en 5 s. Veredicto: aprobado, sin hallazgos. Revisión de prueba, no sustituye a una revisión real de T0. |
| 2026-09-23 | 3 · T0 | Claude Code | Codex (CLI, `npm run revision -Esfuerzo medium`) | 0 | 0 | 1 | 1 | Investigación (sin diff de código): por qué falla `supabase/tests/database/004_storage.test.sql`. Diagnóstico de Claude Code confirmado por Codex: el trigger `storage.protect_delete()` de la imagen real de Storage (`storage-api:v1.72.1`) bloquea cualquier `DELETE` SQL directo sobre `storage.objects` antes de que la RLS actúe; el stub de CI (`scripts/db-ci/supabase_stub.sql`) no tiene ese trigger, por eso allí la prueba pasa. No es estado sucio de la base local (reproducido igual tras `supabase db reset --local --yes`). Sugerencia de Codex (aceptada): no basta con cambiar la aserción a `throws_ok`, porque eso solo probaría que el SQL directo se rechaza, no que la RLS por sí sola impide que B borre ficheros de A — la propiedad de seguridad que la prueba pretendía comprobar sigue sin cubrirse. Pendiente: decidir con Dani cómo reescribir esa cobertura (stub con paridad de trigger, o prueba de extremo a extremo contra la API real de Storage) antes de construir T4. |
| 2026-09-23 | 3 · T0b (ronda 1) | Claude Code | Codex (CLI, `npm run revision -Esfuerzo medium`) | 0 | 1 | 4 | 1 (I) + 2 (S) | Arreglo de `004_storage.test.sql` (`is_empty`→`throws_ok`), disparador `protect_delete` añadido al stub de CI, y cobertura estructural nueva en `001_structure.test.sql`. **Veredicto: no aprobado.** Importante (aceptado): el regex de la nueva aserción estructural (`qual ~ 'foldername' and qual ~ 'auth\.uid'`) daría `ok` aunque la política exigiera la carpeta de OTRO usuario — no demuestra la condición correcta. Sugerencias aceptadas: comparar solo el SQLSTATE `42501` en `throws_ok` (no el mensaje completo, frágil ante cambios de texto de Storage) y precisar en `docs/SEGURIDAD.md` que la comprobación de «sin impacto» es sintáctica (`grep`), no semántica. Sugerencias no accionadas por ser confirmación de lo ya documentado: fidelidad de `storage.protect_delete()` copiada al stub (ya verificada con `pg_get_functiondef` sobre la base real antes de escribir el stub) y que el disparador por sentencia se ejecuta aunque RLS filtre todas las filas (ya era la premisa del diagnóstico). |
| 2026-09-23 | 3 · T0b (ronda 2, tras corregir) | Claude Code | Codex (CLI, `npm run revision -Esfuerzo medium`) | 0 | 0 | 3 | 2 | Reemplazado el regex por una comprobación de igualdad exacta entre el `qual` de `attachments_delete_own` y el de `attachments_select_own` (esta última sí probada de forma conductual en `004_storage.test.sql`). **Veredicto: aprobado**, con 3 sugerencias de matiz (no vacío ni automático: hallazgos concretos con fichero y línea, a diferencia de la revisión de prueba del 22/09). Aceptadas y aplicadas: precisar en el comentario de `004_storage.test.sql` que el disparador es POR SENTENCIA y salta antes de que la RLS filtre filas; precisar en `docs/SEGURIDAD.md` que el acceso de `authenticated` a `storage.buckets` en el stub se comprobó empíricamente (`set role authenticated; select count(*) from storage.buckets` → 1 fila), no solo se infirió del `GRANT`. Limitación reconocida y no resuelta (inherente): la igualdad de `qual` entre ambas políticas solo prueba que son iguales entre sí, no que la condición en sí sea correcta si `attachments_select_own` estuviera también mal — esa garantía final la da la prueba conductual de T4. |
